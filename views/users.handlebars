{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Users</title>
</head>
`<body> --}}
    <h1 align="center">Users</h1><br>

{{#if hasUsers }}
    <ul>
        {{#each users }}
            <li>{{ this.name }}</li>
        {{/each}}
    </ul>
{{ else }}
    <h1 align="center">No Users Found!</h1>
{{/if}}
    {{!-- </body>
</html> --}}